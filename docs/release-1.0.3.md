# MIDA 本地化编辑器 1.0.3

• 将 ZIP 拖入窗口即可识别并导入，语言空间随导入自动创建，移除手动新建与导入按钮。
• 点击译文“确定”后立即保存当前片段，取消后台计时保存，减少重复写入其他片段。
• 仅需修复的句子显示详情及独立编辑按钮，其余句子保持紧凑预览。
• 保存或导出时若有未确定的编辑，自动定位最近编辑的一句。
• 导出仍有待处理问题时，可确认继续导出草稿，保留条目的待处理状态。
